<!DOCTYPE html>

<head>
<link rel="stylesheet" type="text/css" href="aricles.css">
</head>

<body>


<h1 class="p"> Listing all articles </h1>


<table align="center">
	<tr>
		<th>Title</th>
		<th>Description</th>
		<th>Action</th>
	</tr>

	<% @articles.each do |a| %>
	<tr>
		<td><%= a.title %></td>
		<td><%= a.description %></td>
		<td><%= link_to 'Edit', edit_article_path(a) %> |
		<%= link_to 'Show', article_path(a) %> |
		<%= link_to "Delete", article_path(a), method: :delete, data: { confirm: "Are you sure?"}%></td>
	</tr>
	<%end%>

</table>

<h2>For creating a new article press <%= link_to "here", new_article_path %></h2>


</body>

